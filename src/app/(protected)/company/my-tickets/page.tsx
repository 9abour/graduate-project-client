import CompanyTicketsPage from '@/app/(protected)/company/my-tickets/MyTicketsClient';
import React from 'react';

export const metadata = {
  title: 'My Tickets',
};

const page = () => {
  return <CompanyTicketsPage />;
};

export default page;
